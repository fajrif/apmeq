<%= simple_form_for [:admins, @testimonial], :html => { :class => 'form-horizontal' } do |f| %>
	<%= f.error_notification %>
	<div class="form-body">
		<%= f.input :title %>
		<%= f.input :name %>
		<%= f.input :comment, as: :text, input_html: { rows: "20" } %>
		<%= f.input :image, as: :file %>
		<div class="form-actions">
			<div class="row">
				<div class="offset-md-3 col-md-9">
					<%= f.button :submit, :class => 'btn btn-primary' %>
					<% unless @testimonial.new_record? %>
					<%= link_to "Cancel", admins_testimonial_path(@testimonial), :class => 'btn' %>
					<% else %>
					<%= link_to "Cancel", admins_testimonials_path, :class => 'btn' %>
					<% end %>
				</div>
			</div>
		</div>
	</div>
<% end %>
